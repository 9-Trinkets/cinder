//! Reducer integration tests, driven exclusively through cinder-core's public
//! API (`cinder_core::engine::reducer::apply_events` and `WorldEvent`). The
//! module tree lives in `tests/reducer/`.

#[path = "reducer/mod.rs"]
mod reducer;
